{
  "name": "budget-app",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "pnpm --filter @budget-app/web dev",
    "dev:api": "pnpm --filter @budget-app/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "volta": {
    "node": "22.23.2"
  }
}
